# Relay API URL (defaults to /api via Vite dev proxy)
VITE_RELAY_URL=/api

# Deployed MonadStamp contract (required for dashboard)
VITE_CONTRACT_ADDRESS=

# Monad Testnet RPC (optional)
VITE_RPC_URL=https://testnet-rpc.monad.xyz
